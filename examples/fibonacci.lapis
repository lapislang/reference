import <std/io.hl> // import

fn i32 main() { // function declaration
  let i32 a = 1; // basic variable declaration
  let i32 b = 1;
  
  std::cout << 1 << std::endl << 1 << std::endl; // function call, string litteral

  for (let n = 0; n < 100; n++) { // for loop, default integer value
    let tmp = a;
    a = b + a; // mathematical expression
    b = a;
    std::cout << a << std::endl;
  }

  return 0; // return value
}

